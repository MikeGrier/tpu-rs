# Pin the toolchain (and the clippy / rustfmt components) used for local
# development and CI to a single, reproducible version.  This must stay in
# lock-step with [workspace.package].rust-version in Cargo.toml (the MSRV).
[toolchain]
channel    = "1.97.0"
components = ["clippy", "rustfmt"]
